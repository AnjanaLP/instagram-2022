<div class="col">
  <div class="card-body gallery-thumbs">
    <% if post.image.attached? %>
      <%= link_to image_tag(post.image), post %>
    <% else %>
      <%= link_to image_tag(""), post %>
    <% end %>
  </div>
</div>
