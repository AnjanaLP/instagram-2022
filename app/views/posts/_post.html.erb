<div class="col">
  <div class="card-body">
    <% if post.image.attached? %>
      <%= link_to image_tag(post.show_small_image), post %>
    <% else %>
      <%= link_to image_tag(""), post %>
    <% end %>
  </div>
</div>
