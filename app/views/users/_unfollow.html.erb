<%= form_with(model: current_user.active_relationships.find_by(followed_id: object.id),
             html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-secondary #{style}" %>
<% end %>
