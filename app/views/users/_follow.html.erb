<%= form_with(model: current_user.active_relationships.build, local: true) do |f| %>
  <%= hidden_field_tag :followed_id, object.id %>
  <%= f.submit "Follow", class: "btn btn-primary #{style}" %>
<% end %>
